import "./NavBar.css";

const NavBar = (props) => {
  return (
    <div className="navbar-container">
      <h2 className="logo-text">
        Fun<span className="muted-text">Posts</span>
      </h2>
    </div>
  );
};

export default NavBar;
